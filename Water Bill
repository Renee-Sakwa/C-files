#include <iostream>
using namespace std;

int main() {
    int units; double bill;
    cout << "Enter water units consumed: "; cin >> units; 

    if (units <= 30) bill = units * 20; 
    else if (units <= 60) bill = units * 25; 
    else bill = units * 30; [cite: 69]

    cout << "Total water bill: " << bill << " KES" << endl; [cite: 74, 77]
    return 0;
}
